import test from 'node:test';import assert from 'node:assert/strict';import {readLimitedResponse} from '../generated/worker.mjs';
test('crawler response limits reject declared and streamed oversize bodies',async()=>{let canceled=0;const body=()=>new ReadableStream({pull(c){c.enqueue(new Uint8Array(8));},cancel(){canceled++;}});await assert.rejects(readLimitedResponse(new Response(body(),{headers:{'content-length':'20'}}),10),/exceeds/);assert.equal(canceled,1);const response=new Response(body());await assert.rejects(readLimitedResponse(response,10),/exceeds/);assert.equal(canceled,2);assert.equal(response.body.locked,false);assert.equal(await readLimitedResponse(new Response('hello'),5),'hello');});
